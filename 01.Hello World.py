# Hello World!
print("반가워요!") # 반가워요 - 출력하는 기능
# ""(큰따옴표)로 묶은 부분 = 문자열
# print 는 '값을 화면에 출력'
# print처럼 단어 뒤에 () (소괄호)가 붙은 것
# = 함수 (function) -> 정해진 일을 수행하는 단위
# 함수 -> print("반가워요!")와 같이
# 함수 이름 print를 써주고, 괄호 안에 출력할 내용을 넣으면
# 함수가 실행되게 됩니다.
# 함수 실행 -> 함수를 호출한다(call)고 말하기도 함.